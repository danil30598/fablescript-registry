# greetings

Первый демонстрационный пакет FableScript. Экспортирует функцию `hello(string name)`.

```fable
import greetings

print(greetings.hello("Alex"))
```

Установка:

```powershell
fable install greetings
```
